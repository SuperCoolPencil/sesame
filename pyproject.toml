[project]
name = "captiveportallogin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "userpath>=1.9.2",
]

[project.scripts]
sesame = "main:main"
